server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      server:
        webflux:
          routes:
            - id: users-route
              uri: lb://user-service
              predicates:
                - Path=/users/**
            - id: transactions-route
              uri: lb://crypto-trading-sim
              predicates:
                - Path=/transactions/**
            - id: holdings-route
              uri: lb://crypto-trading-sim
              predicates:
                - Path=/holdings/**
            - id: websocket-route
              uri: ws://localhost:8082
              predicates:
                - Path=/ws/**
logging:
  level:
    root: INFO
    org.springframework.cloud.gateway: DEBUG
    reactor.netty.http.client: DEBUG
